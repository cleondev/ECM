{
  "ConnectionStrings": {
    "postgres": "Host=localhost;Port=5432;Database=ecm;Username=postgres;Password=postgres",
    "kafka": "bootstrap.servers=localhost:9092",
    "minio": "Endpoint=http://localhost:9000;AccessKey=minioadmin;SecretKey=minioadmin"
  }
}
